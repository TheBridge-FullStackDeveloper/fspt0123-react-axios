import { styled } from "styled-components";

const Wrapper = styled.section`
  height: 7rem;
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 3rem;
`;

export default {
  Wrapper,
};
